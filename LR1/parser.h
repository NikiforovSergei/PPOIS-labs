#ifndef PARSER_H
#define PARSER_H

#include <iostream>
#include <vector>
#include <string>

using namespace std;

bool exist(string);
vector<string> set_parser(string);
vector<string> sort(vector<string>);
#endif // PARSER_H